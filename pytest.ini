[pytest]
testpaths = tests
markers =
    integration: cross-module and end-to-end scenarios
    slow: heavier scenarios that may take longer than unit tests
filterwarnings =
    ignore:Please use quant_min and quant_max to specify the range for observers\.:UserWarning
    ignore:no signature found for builtin .*PyCapsule.*:UserWarning
